/**
 * PATHCLOCK uses authored CSS and does not require a Tailwind/PostCSS plugin.
 * Keeping this config local prevents Next from inheriting a parent workspace
 * configuration when the project is built from a checkout beneath a home dir.
 */
export default {
  plugins: {},
};
